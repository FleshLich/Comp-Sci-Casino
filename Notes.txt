If the exe does not work, make sure the required files are in the same directory as the exe. As of writing this, the only file needed in the same directory as the exe is the menu templates folder.

If for some reason the exe is not working make sure that there are no other required files